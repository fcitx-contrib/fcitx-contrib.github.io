# Unicode
查询 Unicode 编码或输入 Unicode 字符。

## 设置

### 触发键
按下 `Ctrl+Option+Shift+U` 时，
* 若主选区或剪贴板有内容，将逐字显示 Unicode 编码和描述；
* 输入 Unicode 字符的描述，或 `U+十六进制数字`，匹配的字符会作为候选字出现，选字键为 `Option+数字`。

### 使用十六进制数字输入 Unicode 字符
按下 `Ctrl+Shift+U` 时，直接输入十六进制数字（不需要先导 `U+`），空格上屏对应的 Unicode 字符。
