# macOS 通知
小企鹅有两类通知：
* 提示类，例如拼音按下快捷键切换繁简时，通知用户状态改变，以免无意识误触；
* 动作类，例如首次使用拼音时，询问是否启用云拼音。

## 设置

### 隐藏通知
提示类的通知可以被隐藏。鼠标悬浮在通知上时，可看见 `不要再显示` 按钮，点击后该通知将不再出现。

禁用某项通知（如繁简切换）不影响其他通知（如全角/半角切换）。

若要恢复通知的显示，请在这里将对应项删除。
